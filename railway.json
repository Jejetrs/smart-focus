{
  "build": {
    "builder": "heroku/buildpacks:20"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT app:application --timeout 120 --workers 1 --threads 2 --max-requests 1000",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
